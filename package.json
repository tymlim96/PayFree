{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c green,blue \"npm --prefix frontend start\" \"npm --prefix backend run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
